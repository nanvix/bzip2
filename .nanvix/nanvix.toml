[package]
name = "bzip2"
version = "1.0.8"
nanvix-version = "0.12.506"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["128mb", "256mb"]
